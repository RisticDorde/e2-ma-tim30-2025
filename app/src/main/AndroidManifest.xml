<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MA2025">
        <activity
            android:name=".task.TaskDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".task.TaskCalendarActivity"
            android:exported="false"
            android:label="@string/title_activity_task_calendar"
            android:theme="@style/Theme.MA2025" />
        <activity
            android:name=".task.TaskListActivity"
            android:exported="false" />
        <activity
            android:name=".task.AddTaskActivity"
            android:exported="false" />
        <activity
            android:name=".category.CategoryActivity"
            android:exported="false" />
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>